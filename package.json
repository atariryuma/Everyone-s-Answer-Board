{
  "name": "everyone-s-answer-board",
  "version": "1.0.0",
  "description": "A collaborative answer board using Google Apps Script.",
  "main": "index.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch --passWithNoTests",
    "test:coverage": "jest --coverage --passWithNoTests",
    "lint": "eslint \"src/**/*.{js,gs,ts}\" --fix",
    "format": "prettier --write \"src/**/*.{js,gs,html,ts}\"",
    "check": "npm run test && npm run lint",
    "push": "echo '⚠️ DEPRECATED: Use npm run deploy:safe instead' && clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "echo '⚠️ DEPRECATED: Use npm run deploy:safe instead' && npm run check && npm run push",
    "deploy:staging": "echo '⚠️ DEPRECATED: Use npm run deploy:safe instead' && npm run check && echo 'Staging deployment ready' && npm run push",
    "deploy:prod": "echo '⚠️ DEPRECATED: Use npm run deploy:safe instead' && npm run test:coverage && npm run lint && echo 'Production deployment requires manual approval' && npm run push",
    "deploy:safe": "node scripts/pre-deploy-check.js && ./scripts/safe-deploy.sh",
    "deploy:force": "echo '🚨 WARNING: Force deploy bypasses safety checks!' && clasp push --force",
    "validate": "node scripts/pre-deploy-check.js",
    "backup": "mkdir -p backups/$(date +%Y%m%d_%H%M%S) && cp -r src backups/$(date +%Y%m%d_%H%M%S)/",
    "architecture:test": "node scripts/test-architecture.js || echo 'Architecture test script not yet implemented'",
    "security:review": "node scripts/security-review.js || echo 'Security review script not yet implemented'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atariryuma/Everyone-s-Answer-Board.git"
  },
  "keywords": [
    "gas",
    "google-apps-script"
  ],
  "author": "Ryuma Atari",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/atariryuma/Everyone-s-Answer-Board/issues"
  },
  "homepage": "https://github.com/atariryuma/Everyone-s-Answer-Board#readme",
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.27.1",
    "@babel/preset-env": "^7.28.3",
    "@eslint/js": "^9.34.0",
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^1.0.83",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.16.10",
    "@typescript-eslint/eslint-plugin": "^8.8.0",
    "@typescript-eslint/parser": "^8.8.0",
    "babel-jest": "^30.1.2",
    "eslint": "^9.34.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "googleapis": "^148.0.0",
    "jest": "^29.6.2",
    "jest-environment-jsdom": "^30.1.2",
    "jest-extended": "^4.0.2",
    "jest-junit": "^16.0.0",
    "jsdom": "^26.1.0",
    "nock": "^13.5.5",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2",
    "supertest": "^6.3.4",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
